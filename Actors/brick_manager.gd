extends Node2D

@export var brick_scene: Resource

# const brick_grey = load("res://Assets/Brick_Grey.png")
# const brick_red = load("res://Assets/Brick_Red.png")

func _ready() -> void:
	# var viewportX = get_viewport_rect().size.x
	# var viewportY = get_viewport_rect().size.y / 2

	# for i in 5:
	# 	for j in 5:
	# 		var new_brick = brick_scene.instantiate()
	# 		new_brick.position = Vector2(viewportX * i, viewportY * j)
	# 		add_child(new_brick)
	pass
